version: "3.9"
services:
  autonomous-devops-agent-:
    build: .
    container_name: autonomous-devops-agent-
    ports:
      - "8000:8000"
    restart: unless-stopped
    env_file:
      - .env

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: autonomous-devops-agent-
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: