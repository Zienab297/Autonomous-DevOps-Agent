apiVersion: v1
kind: Service
metadata:
  name: autonomous-devops-agent-
spec:
  selector:
    app: autonomous-devops-agent-
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP